name: Release the lib to Hackage

on:
  push:
    branches:
      - supermajor
      - major
      - minor
      - patch

concurrency:
  group: release
  cancel-in-progress: false

jobs:

  format:
    uses: nikita-volkov/haskell-hackage-lib-github-actions-workflows/.github/workflows/format.yaml@v1.1.0
    secrets: inherit
  
  test:
    uses: nikita-volkov/haskell-hackage-lib-github-actions-workflows/.github/workflows/test.yaml@v1.1.0
    secrets: inherit
  
  release:
    needs:
      - format
      - test
    uses: nikita-volkov/haskell-hackage-lib-github-actions-workflows/.github/workflows/release.yaml@4f9dab5e1c30f19dec79221dbe39f2cbe35dc9c3
    secrets: inherit
    with:
      prefix-tag-with-v: false
